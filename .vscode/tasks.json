{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "electron:build",
            "type": "shell",
            "command": "yarn build",
            "options": {
                "cwd": "${workspaceFolder}/packages/electron"
            },
            "problemMatcher": []
        }
    ]
}