# Afron

## Rules Reference (`_rules/`)

프로젝트의 설계, 구조, 코딩 규칙을 정리한 분석 문서가 `_rules/` 디렉토리에 있다.

| 디렉토리 | 내용 | 언제 참조하는가 |
|---------|------|--------------|
| `_rules/domain/` | 핵심 추상화, 데이터 흐름, 설계 불변식, 확장점, 암묵적 동작 | 새 기능 설계, 기존 로직 수정, 데이터 흐름 파악이 필요할 때 |
| `_rules/navigation/` | 디렉토리 구조, 모듈 의존성, 파일 위치 패턴, 횡단 관심사 | 코드 위치를 찾거나, 새 파일/모듈을 어디에 배치할지 결정할 때 |
| `_rules/convention/` | 네이밍, 에러 처리, API 패턴, 코드 구성, 테스트 규칙 | 새 코드를 작성하거나, 기존 패턴에 맞춰야 할 때 |
