export { ModalBox, Modal, ModalBackground, ModalHeader } from './components';

export { useModal, useModalInstance, ModalProvider } from './context';

export { default as ConfirmModal } from '../../modals/ConfirmModal';